const Discord = require("discord.js");
const date = require('date-and-time');
const fs = require("fs");


module.exports = async (client, member) => {
/*
    let embed = new Discord.MessageEmbed()
    .setColor("#fc7777")
    .setAuthor(`Membre sortie`, member.user.displayAvatarURL)
    .setDescription(`Le membre \`${member.user.username}\` vient de sortir le \`${date.format(member.joinedAt,"HH:mm:ss - DD/MM/YYYY")}\``)

    let logs = member.guild.channels.find(ch => ch.name === "gm-logs")
    if (!logs) return;
    logs.send(embed)
*/
     };